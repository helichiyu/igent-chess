# TODO: AlphaZero Endgame Validation - 2026-07-27

## Requirement Description

Add a small, GPU-enabled AlphaZero-style training experiment to `chess_refactor` without modifying the commemorative parent-directory source files.

The first version is limited to fixed legal endgame positions containing both generals and a red rook. Its goal is to verify the complete training loop, rather than to build a strong full-game Chinese-chess engine.

The implementation must use the refactored `xiangqi` rule engine for legal moves, move application, check detection, terminal-state detection, and repetition handling. It must not reuse the parent-directory `alphazero.m` search or data-accumulation logic.

## Implementation Plan

Create a separate `+alphazero` package that adapts the existing rules to an AlphaZero-style pipeline. Use a small policy-value network, fixed action encoding, legal-action masking, PUCT Monte Carlo tree search, bounded replay data, GPU training, and deterministic candidate evaluation.

The policy maps one board state to scores for all `90 x 90 = 8100` source-target actions. The value estimates the outcome from the current player's perspective. MCTS visit counts provide policy labels, and game outcomes provide value labels.

## Task List

### Phase 1: Experiment Scope and Rule Adapter

- [ ] Task 1: Add centralized AlphaZero experiment parameters in `+alphazero/config.m`, including random seed, GPU execution preference, MCTS simulations, self-play games, maximum plies, replay capacity, batch size, and promotion threshold.
- [ ] Task 2: Define a reproducible fixed set of legal `general + general + red rook` starting positions and validate every position against the `xiangqi` rule engine.
- [ ] Task 3: Implement an AlphaZero state adapter that stores board, side to move, repetition counts, and ply count, and delegates all transitions and terminal checks to `xiangqi` functions.
- [ ] Task 4: Add tests that prove adapter transitions and terminal outcomes match the underlying rule engine.

### Phase 2: State and Action Representation

- [ ] Task 5: Implement a current-player-perspective board encoder using fourteen piece planes.
- [ ] Task 6: Implement reversible move-to-action and action-to-move conversion for the fixed 8100-action space.
- [ ] Task 7: Implement legal-action masks generated exclusively from `xiangqi.legal_moves`.
- [ ] Task 8: Add tests for perspective consistency, move encoding round trips, and illegal-action exclusion.

### Phase 3: Policy-Value Network

- [ ] Task 9: Create a small shared-trunk policy-value `dlnetwork` suitable for short GPU validation runs.
- [ ] Task 10: Implement masked policy normalization, policy cross-entropy loss, value mean squared error, and a single GPU-capable training step.
- [ ] Task 11: Add a model save/load format inside `chess_refactor` that includes model weights and relevant experiment metadata.
- [ ] Task 12: Verify network output shapes, legal masking, finite losses, and one forward/backward GPU training step.

### Phase 4: Monte Carlo Tree Search

- [ ] Task 13: Implement MCTS nodes containing prior probability, visit count, cumulative value, mean value, legal actions, and child references.
- [ ] Task 14: Implement PUCT selection, leaf expansion through the policy-value network, terminal evaluation, and alternating-sign value backpropagation.
- [ ] Task 15: Return a root visit-count policy and a legal selected move; reuse the root subtree between consecutive moves where practical.
- [ ] Task 16: Add tests confirming MCTS always returns legal moves, produces normalized visit policies, and handles terminal states without expansion.

### Phase 5: Self-Play and Replay Data

- [ ] Task 17: Implement temperature-controlled self-play from the fixed endgame set using MCTS.
- [ ] Task 18: Store each sample as encoded state, root visit policy, and final current-player-perspective outcome.
- [ ] Task 19: Implement a fixed-capacity replay buffer with seeded random sampling and chunked MAT-file persistence.
- [ ] Task 20: Verify sample policy sums, values belong to `-1`, `0`, or `1`, replay capacity remains bounded, and short self-play games terminate.

### Phase 6: Training and Evaluation Loop

- [ ] Task 21: Implement `train_endgame_alphazero.m` to alternate self-play generation, bounded replay sampling, GPU training, checkpointing, and candidate evaluation.
- [ ] Task 22: Evaluate candidate and best models over the same fixed endgame suite with colors/roles exchanged and identical maximum-ply rules.
- [ ] Task 23: Promote a candidate only when its configured score threshold is met; otherwise retain the current best model.
- [ ] Task 24: Run a minimal end-to-end experiment and record reproducible settings, output paths, and results.

### Phase 7: Regression Coverage and Optional GUI Integration

- [ ] Task 25: Keep the existing `xiangqi` rule tests passing and add all AlphaZero tests to `run_tests.m`.
- [ ] Task 26: Optionally add an explicit AlphaZero engine choice to `xiangqi.choose_move` only after the training loop is stable; preserve heuristic behavior as the default.
- [ ] Task 27: Update `chess_refactor/README.md` with setup, GPU requirements, experiment scope, command examples, model locations, and known limitations.

## Acceptance Criteria

- All implementation changes are confined to `chess_refactor` and related documentation/TODO files; parent-directory source files remain unmodified.
- Every action selected by MCTS is legal according to `xiangqi.legal_moves`.
- The state encoder and action encoding are deterministic and covered by unit tests.
- A small GPU run completes self-play, replay sampling, policy-value training, checkpointing, and candidate evaluation without unbounded memory growth.
- Existing rule tests and new AlphaZero tests pass through `run_tests`.
- Candidate evaluation is reproducible from a fixed random seed and fixed endgame suite.
- The initial release makes no claim of full-game or tournament-strength Chinese-chess play.

## Execution Record

### 2026-07-27

- Created the plan after comparing the original parent-directory prototype with a MCTS-based policy-value design.
- Scope fixed to a small endgame validation experiment before any full-game self-play work.
- No `chess_refactor` source files have been changed yet.
