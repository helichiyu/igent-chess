# TODO: Curated Endgame Challenge Set - 2026-07-27

## Requirement Description

Replace the requirement to reproduce four uniquely authoritative classic endgames with a curated Chinese-chess endgame challenge set. Train one shared AlphaZero policy-value model on the approved scenarios, evaluate every scenario separately, and later expose the approved scenarios in the GUI as endgame challenges.

The source of truth for the initial candidate set is the downloaded, version-pinned file `chess_refactor/references/classic_endgames/candidate_endgames_ZDSxbj_ChineseChess.ts`. The project must describe these as curated challenge scenarios, not as canonical historical reproductions.

## Assumptions

- The initial candidate set excludes the source entry named `test`.
- The initial candidate scenarios are `七星聚会`, `马跃檀溪`, `炮炸两狼关`, `小征东`, `蚯蚓降龙`, `大九连环`, `带子入朝`, `征西`, and `野马操田`.
- A scenario is admitted only when the existing `xiangqi` rule engine confirms that its starting board is legal, non-terminal, and has at least one legal move for the configured side.
- The GUI challenge objective is the configured final win or draw result; it does not require reproducing a historical principal variation.

## Implementation Plan

Convert the chosen source records into versioned MATLAB scenario data. Validate every position using the existing rules. Use a single shared policy-value network for balanced self-play across the admitted scenarios, record metrics separately by scenario, and promote a checkpoint only when it meets the configured scenario-level criteria. Add GUI challenge selection after smoke training has verified the scenario data and model interface.

## Task List

### Phase 1: Scenario Import and Validation

- [ ] Task 1: Add a MATLAB scenario-data format containing identifier, display name, board, side to move, player side, expected result, maximum plies, and source metadata.
- [ ] Task 2: Convert the nine selected source records into the project board encoding without changing their coordinates or material.
- [ ] Task 3: Validate all converted boards through `xiangqi.legal_moves` and `xiangqi.game_state`; reject or document any source position that fails.
- [ ] Task 4: Specify an explicit expected final result and player role for every admitted scenario before training.
- [ ] Task 5: Add unit tests locking the approved scenario piece placements, starting side, and legal move availability.

### Phase 2: Scenario-Aware Shared Training

- [ ] Task 6: Replace validation-only start positions with the admitted curated scenarios while retaining the existing simple positions as test fixtures only.
- [ ] Task 7: Sample self-play evenly across scenarios and store scenario identifiers in replay samples and persisted chunks.
- [ ] Task 8: Run candidate-versus-best evaluation separately for each scenario and report wins, draws, losses, average plies, and repetition draws.
- [ ] Task 9: Add configurable scenario-level promotion thresholds; do not promote a shared checkpoint solely because of aggregate score.
- [ ] Task 10: Persist checkpoint metadata containing source version, admitted scenario identifiers, per-scenario metrics, and training settings.

### Phase 3: Smoke Training and Review

- [ ] Task 11: Run a short GPU smoke experiment over every admitted scenario and verify self-play termination, valid labels, and metric reporting.
- [ ] Task 12: Review smoke results, remove unsuitable scenarios or adjust per-scenario maximum plies, then freeze scenario-data version 1.
- [ ] Task 13: Start the longer shared GPU training run only after the frozen scenario set and smoke metrics are accepted.

### Phase 4: GUI Challenge

- [ ] Task 14: Add an English endgame-challenge selector listing only admitted scenarios, player side, objective, and maximum plies.
- [ ] Task 15: Start the selected board and call MCTS with the accepted shared-model checkpoint for AI moves.
- [ ] Task 16: Show challenge success or failure using only the configured final result and maximum-ply rule.
- [ ] Task 17: Preserve existing normal-game GUI modes and heuristic behavior without changes.
- [ ] Task 18: Add GUI-independent integration tests for scenario loading, challenge transitions, and result adjudication.

## Acceptance Criteria

- Every enabled challenge has a source-traceable board and passes the project rule-engine validation.
- One shared model trains from all admitted scenarios, with separate metrics for every scenario.
- No scenario is presented as a canonical historical layout unless separately verified.
- A smoke run completes every admitted scenario without invalid actions, invalid labels, or unbounded replay growth.
- The later GUI challenge succeeds only on the configured final win or draw result.

## Execution Record

### 2026-07-27

- Created an earlier four-classic-endgame verification plan and downloaded two public candidate data files into `chess_refactor/references/classic_endgames/`.
- The original four-name plan was superseded because the downloaded sources contain simplified or divergent layouts and no verified `千里独行` coordinate record.
- Re-scoped the project from four historically fixed endgames to a curated, source-traceable challenge set.
- Selected the nine named non-test records from the downloaded `ZDSxbj/ChineseChess` candidate source as the initial import set.
- No training scenarios or GUI behaviors have changed yet.
