# TODO: AlphaZero Exploration Restart - 2026-07-28

## Requirement Description

Discard the collapsed full-game training artifacts and restart from a clean model after adding the minimum AlphaZero-style exploration and promotion controls.

## Implementation Plan

Use draw value zero, matching the zero-sum MCTS and the AlphaZero paper. Add root-only Dirichlet prior noise during self-play, retain random visit-count sampling for the first 30 plies, use a stricter candidate promotion threshold, and run more role-swapped evaluation games. Keep evaluation and GUI search deterministic without root noise.

## Task List

### Phase 1: Training Controls

- [x] Add root-only Dirichlet exploration, 64-search default, 30-ply sampling, zero draw value, and 55% promotion threshold.
- [x] Add regression tests for root noise, deterministic evaluation, and rejection of a 50% all-draw candidate.

### Phase 2: Clean Restart

- [x] Delete all existing generated model, replay, progress, and log artifacts.
- [x] Initialize a new versioned full-game model and run one GPU smoke iteration.

## Acceptance Criteria

- Self-play has root exploration noise; evaluation and GUI do not.
- A 50% all-draw candidate is not promoted.
- The new experiment begins at iteration 1 with no old replay data.

## Execution Record

### 2026-07-28

- Identified that a symmetric negative draw label is incompatible with zero-sum MCTS backpropagation; reverted to zero draw value for the restarted experiment.
- Deleted prior models, all archived model artifacts, replay chunks, progress files, and logs as requested.
- GPU smoke training completed from a clean state at iteration 1: 20 plies, finite loss 3.6165, 0.500 evaluation score, and no checkpoint promotion under the 55% threshold.
- Added per-game Command Window progress for model evaluation, including role, completed plies, and result.
